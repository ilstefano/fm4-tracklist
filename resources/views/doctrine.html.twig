{% extends 'layout.html.twig' %}
{% set active = 'doctrine' %}

{% block content %}

    <h1>{{ 'Last FM4 Tracklist'|trans }}</h1>

    <ul>
        {% for track in tracks %}
            <li>{{ track.zeit }}, {{ track.interpret}} : {{ track.title }}, Count: {{ track.count }}</li>
        {% endfor %}
    </ul>

{% endblock %}
